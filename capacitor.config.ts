import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.waarheiddurvendoen.app',
  appName: 'waarheid-durven-doen-app',
  webDir: 'build'
};

export default config;
